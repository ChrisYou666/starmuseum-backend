spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/starmuseum?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai}
    username: ${DB_USER:starmuseum}
    password: ${DB_PASSWORD:starmuseum123}
    driver-class-name: com.mysql.cj.jdbc.Driver

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://your-domain.com}

logging:
  level:
    root: info
